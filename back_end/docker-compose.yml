services:
  postgres:
    image: postgres:15
    container_name: my-postgres

#  keycloak:
#    image: quay.io/keycloak/keycloak:21.1.1
#    container_name: keycloak
#    environment:
      # Utilisateurs admin Keycloak
#      KEYCLOAK_ADMIN: ${KEYCLOAK_ADMIN_USER}
#      KEYCLOAK_ADMIN_PASSWORD: ${KEYCLOAK_ADMIN_PASS}
#      # Si tu veux stocker les données Keycloak dans PostgreSQL plutôt que H2
#      KC_DB: postgres
#      KC_DB_URL_HOST: postgres
#      KC_DB_URL_PORT: "5432"
#      KC_DB_URL_DATABASE: mydb
#      KC_DB_USERNAME: postgres
#      KC_DB_PASSWORD: postgres
#    command:
#      - start-dev
#      - --import-realm           # si tu as un export de realm à importer
#    ports:
#      - "8080:8080"
#    depends_on:
#      - postgres

  app:
    build: .
    container_name: my-app
    ports:
      - "3000:3000"
    depends_on:
      - postgres
    env_file:
      - .env
    environment:
      DATABASE_URL: ${CONTAINER_DATABASE_URL}
      DIRECT_URL:  ${CONTAINER_DATABASE_URL}
      KEYCLOAK_BASE_URL: ${KEYCLOAK_BASE_URL}
      KEYCLOAK_REALM:     ${KEYCLOAK_REALM}
      KEYCLOAK_ADMIN_USER: ${KEYCLOAK_ADMIN_USER}
      KEYCLOAK_ADMIN_PASS: ${KEYCLOAK_ADMIN_PASS}
      KEYCLOAK_CLIENT_ID: ${KEYCLOAK_CLIENT_ID}
    volumes:
      - .:/app
    working_dir: /app
    depends_on:
      - postgres
    environment:
      - DATABASE_URL=postgresql://postgres:postgres@postgres:5432/mydb
    command: sh -c "npx prisma migrate dev --name init && npm run dev"

volumes:
  pgdata:
